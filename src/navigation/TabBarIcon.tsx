// Simple placeholder for icons, replace with a real icon library like @expo/vector-icons
const TabBarIcon = ({ name, color, size }: { name: string; color: string; size: number }) => {
    // In a real app, you'd return an <Icon> component here based on the name
    // e.g., <Ionicons name={name} size={size} color={color} />
    // For now, just returning null or a placeholder text
    // return <Text style={{ color: color }}>{name.substring(0, 3)}</Text>;
    return null; // Keep it clean initially
  }; 